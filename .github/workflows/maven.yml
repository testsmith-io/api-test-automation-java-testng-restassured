name: API Test Automation with Java, TestNG and Rest Assured

on:
  workflow_dispatch:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  schedule:
    - cron: 0 20 * * 6

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - name: Set up JDK 17
        uses: actions/setup-java@v5
        with:
          distribution: 'temurin'
          java-version: '17'
          cache: 'maven'
      - name: Check formatting
        run: mvn spotless:check
      - name: Run all tests
        run: mvn clean test
      - name: Publish Test Report
        if: always()
        uses: scacap/action-surefire-report@v1.13.0
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          fail_on_test_failures: true